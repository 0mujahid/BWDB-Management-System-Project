package BWDBManagementSystem;

import java.sql.Connection;

public class WorkProgress {
 private final Connection connection;
 public WorkProgress(Connection connection) {
     this.connection = connection;
     
 }
}